daemon off;

error_log tests/log/nginx-error.log;
pid /tmp/nginx.pid;

events {
    worker_connections 16;
}

http {
    include mime.types;
    include api-nginx.conf;
    include rest-nginx.conf;
    include mission-nginx.conf;
}
