#cop vars,example
#cop execresult,example
#cop Function execresult,example

body common control 
{
bundlesequence => { "test" };
}

bundle agent test 
{
vars:
    any::
        "tempdir" string => execresult("/bin/mktemp -d", "noshell");
reports:
    linux::
        "tempdir: ${tempdir}";
}