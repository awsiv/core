#cop usemodule,example
#cop Function usemodule,example

#
# Test module execution as class function
#

body common control
   {
   any::

      bundlesequence  => { 
                         test
                         };
   }

###################################################################

bundle agent test

{
classes:

  "done" expression => usemodule("module:getusers","");

commands:

  "/bin/echo promiser text" args => "test $(user)";
}
